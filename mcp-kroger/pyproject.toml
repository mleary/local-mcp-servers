[project]
name = "mcp-kroger"
version = "0.1.0"
description = "Kroger MCP Server for grocery shopping with context mapping"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.13.1",
    "python-dotenv>=1.1.1",
    "fastmcp>=2.11.3"
]
